# 다이소 매장 내 상품 위치 안내 서비스 - Frontend 구현설계서

> **프로젝트명:** 어디다있소 (Where is it in Daiso?)  
> **기술 스택:** Next.js 14 (App Router) + TypeScript + TailwindCSS  
> **작성일:** 2026-02-09

---

## 1. 프로젝트 개요

### 1.1 서비스 목표
다이소 매장 내 키오스크에서 음성/텍스트 검색을 통해 상품 위치를 안내하고, QR 코드를 통해 모바일과 연동하여 3단계 위치 안내(텍스트 → QR → 화살표/AR)를 제공합니다.

### 1.2 사용자 플로우 (User Flow)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        태블릿 키오스크 (Tablet Kiosk)                             │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌──────────────┐   Stage 1. 입력 및 의도 분석                                   │
│   │ 1. 시작      │────────────────────────────────────────┐                      │
│   │ (태블릿 실행) │                                        │                      │
│   └──────┬───────┘                                        │                      │
│          ▼                                                ▼                      │
│   ┌──────────────────┐        No         ┌───────────────────────────┐          │
│   │ 2. 상품검색       │───────────────────│ 3-1. 위치 안내 불가 안내   │          │
│   │ (음성/텍스트 입력) │                   │ (질문 예시 제안내 또는     │          │
│   └──────┬───────────┘    ┌──────────────│  직원 콜)                  │          │
│          ▼                │              └───────────────────────────┘          │
│   ┌────────────────┐      │                                                     │
│   │ 3. 의도분석     │──────┘                                                     │
│   │ (상품위치확인)  │ Yes                                                        │
│   └──────┬─────────┘                                                            │
│          ▼                                                                      │
│   ┌────────────────┐                                                            │
│   │ 4. 키워드 추출 │                                                            │
│   └──────┬─────────┘                                                            │
│          │                                                                      │
│          │        Stage 2. 검색                                                 │
│          ▼                                                                      │
│   ┌────────────────┐                                                            │
│   │ 5. 검색실행    │                                                            │
│   └──────┬─────────┘                                                            │
│          ▼                                                                      │
│   ┌────────────────┐                                                            │
│   │ 6. 검색 결과   │                                                            │
│   │    판별       │                                                            │
│   └──────┬─────────┘                                                            │
│          ▼                                                                      │
│   ┌────────────────────┐                                                        │
│   │ 7. 결과화면 출력   │                                                        │
│   │ (예시3, 없음1)     │                                                        │
│   └──────┬─────────────┘                                                        │
│          │                                                                      │
│          │        Stage 3. 결과안내                                             │
│          ▼                                                                      │
│   ┌────────────────┐                                                            │
│   │ 8. 상품 선택   │                                                            │
│   └──────┬─────────┘                                                            │
│          │                                                                      │
│          ▼       상품 선택                                                       │
│   ┌─────────────────────┐                                                       │
│   │ 9. 매대 번호        │                                                       │
│   │ 및 QR 코드 안내     │─────────────── Stage 4. 모바일 연동 ──────────────────┐ │
│   └─────────┬───────────┘                                                      ││ │
│             │ 원하는 상품 없음 선택                     ┌────────────────────────┼┘ │
│             ▼                                         │ 모바일 (스마트폰)       │  │
│   ┌─────────────────────┐                             │                        │  │
│   │ 13. 태블릿 종료     │                             │ ┌──────────────────┐   │  │
│   │ (자동/다음 타자 터치)│                             │ │ 10. 모바일 카메라 │   │  │
│   └─────────────────────┘                             │ │ 또는 QR 리더 실행 │   │  │
│                                                       │ └────────┬─────────┘   │  │
└───────────────────────────────────────────────────────│──────────│─────────────┘  │
                                                        │          ▼               │
                                                        │ ┌──────────────────┐     │
                                                        │ │ 11. QR 스캔      │     │
                                                        │ └────────┬─────────┘     │
                                                        │          │               │
                                                        │    ┌─────┴─────┐         │
                                                        │    ▼           ▼         │
                                                        │ ┌──────────┐ ┌──────────┐│
                                                        │ │12-1. 지도│ │12-2. 내비││
                                                        │ │  보며    │ │  보며    ││
                                                        │ │  이동    │ │  이동    ││
                                                        │ └────┬─────┘ └────┬─────┘│
                                                        │      └─────┬──────┘      │
                                                        │            ▼             │
                                                        │ ┌──────────────────┐     │
                                                        │ │ 14. 모바일 안내  │     │
                                                        │ │     종료        │     │
                                                        │ └──────────────────┘     │
                                                        └──────────────────────────┘
```

### 1.3 시스템 아키텍처

```
                          ┌─────────────────────────────────────┐
                          │            Clients                  │
                          │                                     │
                          │  ┌────────────────┐ ┌─────────────┐│
                          │  │ Next.js Kiosk  │ │ React Mobile││
                          │  │    PWA         │ │     AR      ││
                          │  └───────┬────────┘ └──────┬──────┘│
                          └──────────┼─────────────────┼───────┘
                                     │                 │
                                     ▼                 ▼
                          ┌─────────────────────────────────────┐
                          │      AWS ALB / API Gateway          │
                          └─────────────────┬───────────────────┘
                                            │
                          ┌─────────────────▼───────────────────┐
                          │       FastAPI Backend Cluster       │
                          │                                     │
                          │  ┌──────────────────────────────┐   │
                          │  │       Main API Router        │   │
                          │  └──────────────┬───────────────┘   │
                          │                 │                   │
                          │    ┌────────────┴────────────┐      │
                          │    ▼                         ▼      │
                          │ ┌──────────────────┐ ┌────────────┐ │
                          │ │  AI Agent Layer  │ │Core Services│ │
                          │ │                  │ │            │ │
                          │ │ ┌──────────────┐ │ │┌──────────┐│ │
                          │ │ │  Supervisor  │ │ ││Google STT││ │
                          │ │ │ (LangGraph)  │ │ ││ Whisper  ││ │
                          │ │ └──────┬───────┘ │ │└──────────┘│ │
                          │ │        │         │ │            │ │
                          │ │        ▼         │ │┌──────────┐│ │
                          │ │ ┌──────────────┐ │ ││   QR     ││ │
                          │ │ │Intent+Keyword│ │ ││Generator ││ │
                          │ │ │Gemini 2.0    │ │ │└──────────┘│ │
                          │ │ └──────┬───────┘ │ └────────────┘ │
                          │ │        ▼         │                │
                          │ │ ┌──────────────┐ │                │
                          │ │ │Hybrid Search │ │                │
                          │ │ │BM25 + Vector │ │                │
                          │ │ └──────┬───────┘ │                │
                          │ │        ▼         │                │
                          │ │ ┌──────────────┐ │                │
                          │ │ │LLM Re-ranker │ │                │
                          │ │ │Gemini 20 Fl │ │                │
                          │ │ └──────────────┘ │                │
                          │ └──────────────────┘                │
                          └─────────────────┬───────────────────┘
                                            │
                          ┌─────────────────▼───────────────────┐
                          │       Data Infrastructure           │
                          │                                     │
                          │  ┌────────────┐ ┌───────┐ ┌────────┐│
                          │  │PostgreSQL  │ │ Redis │ │ChromaDB││
                          │  │  (Meta)    │ │(Cache)│ │(Vector)││
                          │  └────────────┘ └───────┘ └────────┘│
                          └─────────────────────────────────────┘
```

---

## 2. 디자인 시스템 (Design System)

### 2.1 레이아웃 스펙

| 디바이스 | 해상도 | 용도 |
|---------|--------|------|
| **Kiosk (Tablet)** | 1280 x 800 px | 매장 내 터치 키오스크 |
| **Mobile** | 390 x 844 px | QR 스캔 후 모바일 뷰 |

### 2.2 컬러 팔레트 (CSS Variables)

```css
:root {
  /* Brand Colors */
  --daiso-red: #E60012;
  --daiso-red-light: #FFEBEE;
  --daiso-red-shadow: rgba(230, 0, 18, 0.25);
  
  /* Text Colors */
  --text-primary: #333333;
  --text-secondary: #666666;
  --text-muted: #999999;
  
  /* Background Colors */
  --bg-base: #F5F5F5;
  --bg-surface: #FFFFFF;
  --bg-input: #F5F5F5;
  
  /* Border Colors */
  --border-light: #E0E0E0;
  --border-default: #E5E5E5;
  
  /* Shadow */
  --shadow-sm: 0 1px 4px rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.08);
}
```

### 2.3 타이포그래피

```css
/* Font Family */
--font-family: 'SUITE', 'Pretendard', -apple-system, sans-serif;

/* Font Sizes */
--text-xs: 12px;    /* 캡션, 라벨 */
--text-sm: 14px;    /* 보조 텍스트 */
--text-base: 16px;  /* 본문 */ 
--text-lg: 18px;    /* 부제목 */
--text-xl: 20px;    /* 제목 */
--text-2xl: 24px;   /* 로고 텍스트 */
--text-3xl: 28px;   /* 강조 텍스트 */
--text-4xl: 48px;   /* 대형 헤딩 */

/* Font Weights */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
--font-extrabold: 800;
```

### 2.4 컴포넌트 스펙

| 컴포넌트 | 높이 | 모서리 | 그림자 |
|---------|------|--------|--------|
| Header | 80px | 0 | shadow-sm |
| Bottom Navigation | 60px | 0 | shadow-md (위로) |
| Search Input | 64px | 32px | shadow-sm |
| Voice Button | 80px | 40px (원형) | shadow-lg + red glow |
| Product Card | auto | 16px | shadow-md |
| Category Button | 44px | 8px | - |
| Action Button | 56px | 28px | shadow-md |

---

## 3. 프로젝트 구조

```
daiso-kiosk-frontend/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── layout.tsx                # 루트 레이아웃
│   │   ├── page.tsx                  # 홈 화면 (Voice Home)
│   │   ├── globals.css               # 전역 스타일
│   │   │
│   │   ├── (kiosk)/                  # 키오스크 라우트 그룹
│   │   │   ├── layout.tsx            # 키오스크 공통 레이아웃
│   │   │   ├── voice-input/          # 음성 입력 화면
│   │   │   │   └── page.tsx
│   │   │   ├── search/               # 검색 결과 화면
│   │   │   │   └── page.tsx
│   │   │   ├── category-map/         # 카테고리 지도
│   │   │   │   └── page.tsx
│   │   │   └── not-found/            # 결과 없음 화면
│   │   │       └── page.tsx
│   │   │
│   │   └── mobile/                   # 모바일 라우트
│   │       ├── layout.tsx            # 모바일 레이아웃
│   │       ├── page.tsx              # QR 랜딩
│   │       ├── map/                  # 지도 보기
│   │       │   └── page.tsx
│   │       └── ar/                   # AR 네비게이션
│   │           └── page.tsx
│   │
│   ├── components/                   # 공통 컴포넌트
│   │   ├── ui/                       # 기본 UI 컴포넌트
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Icon.tsx
│   │   │   └── index.ts
│   │   │
│   │   ├── kiosk/                    # 키오스크 전용 컴포넌트
│   │   │   ├── Header.tsx            # 상단 헤더
│   │   │   ├── BottomNavigation.tsx  # 하단 네비게이션
│   │   │   ├── VoiceButton.tsx       # 음성 입력 버튼
│   │   │   ├── SearchBar.tsx         # 검색 바
│   │   │   ├── ProductCard.tsx       # 상품 카드
│   │   │   ├── ProductList.tsx       # 상품 목록 (Left Panel)
│   │   │   ├── CategorySidebar.tsx   # 카테고리 사이드바
│   │   │   ├── FloorMap.tsx          # 층별 지도
│   │   │   ├── QRCodeDisplay.tsx     # QR 코드 표시
│   │   │   ├── Waveform.tsx          # 음성 파형 애니메이션
│   │   │   └── CarouselBanner.tsx    # 캐러셀 배너
│   │   │
│   │   └── mobile/                   # 모바일 전용 컴포넌트
│   │       ├── MobileHeader.tsx
│   │       ├── BottomSheet.tsx
│   │       ├── MapView.tsx
│   │       └── AROverlay.tsx
│   │
│   ├── hooks/                        # 커스텀 훅
│   │   ├── useVoiceInput.ts          # 음성 입력 훅 (Web Speech API)
│   │   ├── useSearch.ts              # 검색 API 훅
│   │   ├── useLocation.ts            # 위치 정보 훅
│   │   └── useQRCode.ts              # QR 코드 생성 훅
│   │
│   ├── services/                     # API 서비스
│   │   ├── api.ts                    # Axios 인스턴스
│   │   ├── searchService.ts          # 검색 API
│   │   ├── locationService.ts        # 위치 API
│   │   └── sessionService.ts         # 세션 관리
│   │
│   ├── store/                        # 상태 관리 (Zustand)
│   │   ├── useSearchStore.ts         # 검색 상태
│   │   ├── useSessionStore.ts        # 세션 상태
│   │   └── useUIStore.ts             # UI 상태
│   │
│   ├── types/                        # TypeScript 타입
│   │   ├── product.ts
│   │   ├── location.ts
│   │   ├── search.ts
│   │   └── api.ts
│   │
│   ├── utils/                        # 유틸리티
│   │   ├── cn.ts                     # className 헬퍼
│   │   ├── formatters.ts             # 포맷팅 함수
│   │   └── constants.ts              # 상수 정의
│   │
│   └── assets/                       # 정적 자산
│       ├── fonts/
│       └── images/
│
├── public/
│   ├── icons/
│   └── maps/                         # 층별 지도 SVG/이미지
│
├── tailwind.config.ts
├── next.config.js
├── tsconfig.json
└── package.json
```

---

## 4. 화면별 상세 설계

### 4.1 Screen 1: 홈 화면 (Voice Home)

**경로:** `/`

**레이아웃 구성:**
```
┌──────────────────────────────────────────────────────────────────┐
│ Header (80px)                                                    │
│ ┌────────────┐                                    ┌────────────┐ │
│ │ 🟥 어디다있소 │                                  │ 12:30 PM 📶 │ │
│ └────────────┘                                    └────────────┘ │
├──────────────────────────────────────────────────────────────────┤
│ Voice Search Section (160px)                                     │
│         ┌─────────────────────────────────────────────────┐      │
│    🎤   │  찾으시는 상품을 말씀해주세요               🔍   │      │
│  (80px) │                                                  │      │
│         └─────────────────────────────────────────────────┘      │
├──────────────────────────────────────────────────────────────────┤
│ Carousel Section (340px)                                         │
│ ┌────────────────────────────────────────────────────────────┐   │
│ │                   프로모션 캐러셀 배너                      │   │
│ │                      (1100 x 300)                          │   │
│ └────────────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────────┤
│ Category Shortcuts (fill)                                        │
│       ┌────┐    ┌────┐    ┌────┐    ┌────┐                       │
│       │ 🏠 │    │ 📦 │    │ 🗺️ │    │ 🎧 │                       │
│       │ 홈 │    │카테│    │매장│    │ 고객│                       │
│       │    │    │고리│    │지도│    │센터│                       │
│       └────┘    └────┘    └────┘    └────┘                       │
└──────────────────────────────────────────────────────────────────┘
```

**핵심 컴포넌트:**
- `Header`: 로고 + 시간/상태 아이콘
- `VoiceButton`: 마이크 버튼 (pulse 애니메이션)
- `SearchBar`: 텍스트 검색 입력
- `CarouselBanner`: 프로모션 슬라이드
- `CategoryShortcuts`: 4개 아이콘 그리드

**상호작용:**
1. 마이크 버튼 클릭 → `/voice-input` 이동
2. 검색바 입력 후 엔터 → `/search?q={query}` 이동
3. 카테고리 클릭 → `/category-map?category={category}` 이동

---

### 4.2 Screen 2: 음성 입력 (Voice Input)

**경로:** `/voice-input`

**레이아웃 구성:**
```
┌──────────────────────────────────────────────────────────────────┐
│ Header (80px)                                                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│                      듣고 있습니다...                             │
│                                                                  │
│                    ║║║│║║║│║│║║║│║║                               │
│                      (Waveform Animation)                        │
│                                                                  │
│                   "세탁 세제 어디 있어요?"                         │
│                                                                  │
│               ┌────────────┐    ┌────────────┐                   │
│               │    취소    │    │    확인    │                   │
│               └────────────┘    └────────────┘                   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**핵심 컴포넌트:**
- `Waveform`: 13개 바 애니메이션 (실시간 오디오 시각화)
- 사용자 음성 텍스트 실시간 표시

**상호작용:**
1. Web Speech API로 음성 인식
2. 확인 클릭 → `/search?q={transcript}` 이동
3. 취소 클릭 → `/` 복귀

---

### 4.3 Screen 3: 검색 결과 (Search Results)

**경로:** `/search?q={query}`

**레이아웃 구성:**
```
┌──────────────────────────────────────────────────────────────────┐
│ Header (80px)                                                    │
├──────────────────────────────────────────────────────────────────┤
│ Main Content                                                     │
│ ┌────────────────────┬───────────────────────────────────────┐   │
│ │ Left Panel (320px) │ Right Panel - Map (fill)              │   │
│ │                    │                                       │   │
│ │ ┌────────────────┐ │    B1                   B2            │   │
│ │ │ 검색 결과 3개  │ │  ┌──────────┐        ┌──────────┐     │   │
│ │ └────────────────┘ │  │          │        │          │     │   │
│ │                    │  │    ●     │        │          │     │   │
│ │ ┌────────────────┐ │  │  매대위치 │        │          │     │   │
│ │ │ 1. 규조토 욕실 │ │  │          │        │          │     │   │
│ │ │    매트       │ │  └──────────┘        └──────────┘     │   │
│ │ │    📍 B1-B01  │ │                                       │   │
│ │ │    5,000원    │ │                                       │   │
│ │ └────────────────┘ │                                       │   │
│ │                    │                                       │   │
│ │ ┌────────────────┐ │                                       │   │
│ │ │ 2. 다용도 버너 │ │                                       │   │
│ │ └────────────────┘ │                                       │   │
│ │                    │                                       │   │
│ │ ┌────────────────┐ │                                       │   │
│ │ │ 3. 빈기름청소술│ │                                       │   │
│ │ └────────────────┘ │                                       │   │
│ │                    │                                       │   │
│ │ ┌────────────────┐ │                                       │   │
│ │ │ QR 스캔하여   │ │                                       │   │
│ │ │ 모바일 스캔   │ │                                       │   │
│ │ ├────────────────┤ │                                       │   │
│ │ │[카카오톡 보내기]│ │                                       │   │
│ │ └────────────────┘ │                                       │   │
│ └────────────────────┴───────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────────┤
│ Bottom Navigation (60px)                                         │
│         🔍 검색        🗺️ 매장지도        👤 My                   │
└──────────────────────────────────────────────────────────────────┘
```

**핵심 컴포넌트:**
- `ProductList`: 검색 결과 목록 (스크롤 가능)
- `ProductCard`: 개별 상품 정보 (이미지, 이름, 위치, 가격)
- `FloorMap`: B1/B2 층 지도 (선택된 상품 위치 표시)
- `QRCodeDisplay`: QR 코드 + 카카오톡 공유 버튼
- `BottomNavigation`: 검색/매장지도/My 탭

**상호작용:**
1. 상품 카드 클릭 → 해당 위치가 지도에 강조 표시
2. QR 코드 표시 → 모바일 스캔 유도
3. 카카오톡 버튼 → 딥링크 공유

---

### 4.4 Screen 4: 카테고리 지도 (Category Map)

**경로:** `/category-map`

**레이아웃 구성:**
```
┌──────────────────────────────────────────────────────────────────┐
│ Header (80px)                                                    │
├──────────────────────────────────────────────────────────────────┤
│ Main Content                                                     │
│ ┌────────────────────────────────────────────┬──────────────────┐│
│ │ Map Container (fill)                       │Category Sidebar  ││
│ │                                            │(140px)           ││
│ │  ┌─────────────────┐ ┌─────────────────┐   │                  ││
│ │  │ B1 Floor Card   │ │ B2 Floor Card   │   │ ┌──────────────┐ ││
│ │  │                 │ │                 │   │ │  🔴 전체     │ ││
│ │  │  ┌────┬────┐   │ │  ┌────┬────┐   │   │ └──────────────┘ ││
│ │  │  │뷰티│화장│   │ │  │음식│주방│   │   │ ┌──────────────┐ ││
│ │  │  ├────┼────┤   │ │  ├────┼────┤   │   │ │  🌸 뷰티     │ ││
│ │  │  │생활│문구│   │ │  │수납│청소│   │   │ └──────────────┘ ││
│ │  │  └────┴────┘   │ │  └────┴────┘   │   │ ┌──────────────┐ ││
│ │  │                 │ │                 │   │ │  🍳 주방     │ ││
│ │  │  ▲ 입구        │ │                 │   │ └──────────────┘ ││
│ │  └─────────────────┘ └─────────────────┘   │ ┌──────────────┐ ││
│ │                                            │ │  🛁 욕실     │ ││
│ │                                            │ └──────────────┘ ││
│ │                                            │ ┌──────────────┐ ││
│ │                                            │ │  📝 문구     │ ││
│ │                                            │ └──────────────┘ ││
│ │                                            │       ...        ││
│ └────────────────────────────────────────────┴──────────────────┘│
├──────────────────────────────────────────────────────────────────┤
│ Bottom Navigation (60px)                                         │
│     🗺️ 카테고리지도     🔍 상품검색     🛒 장바구니              │
└──────────────────────────────────────────────────────────────────┘
```

**핵심 컴포넌트:**
- `FloorMap`: B1/B2 층별 매대 배치도
- `CategorySidebar`: 9개 카테고리 버튼 (스크롤)
- `ZoneHighlight`: 선택된 카테고리 영역 강조

**상호작용:**
1. 카테고리 버튼 클릭 → 해당 영역 색상 강조
2. 지도 영역 터치 → 해당 카테고리 상품 목록 표시

---

### 4.5 Screen 5: 결과 없음 (Not Found)

**경로:** `/not-found`

**레이아웃 구성:**
```
┌──────────────────────────────────────────────────────────────────┐
│ Header (60px) - 간소화                                           │
│   ← 뒤로              검색 결과                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│                           😕                                     │
│                                                                  │
│                      죄송합니다                                   │
│                                                                  │
│              해당 상품의 위치를 찾을 수 없습니다                   │
│                                                                  │
│               ┌─────────────────────────────────┐                │
│               │ 💡 이렇게 검색해 보세요:         │                │
│               │                                 │                │
│               │ • "세탁세제 어디 있어요?"        │                │
│               │ • "2층 매대 위치"               │                │
│               └─────────────────────────────────┘                │
│                                                                  │
│           ┌─────────────┐    ┌─────────────────┐                 │
│           │ 직원 호출   │    │ 🔍 다시 검색하기  │                 │
│           └─────────────┘    └─────────────────┘                 │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**핵심 컴포넌트:**
- `TipCard`: 검색 팁 안내
- `Button`: 직원 호출 / 다시 검색

---

### 4.6 Screen 6: 모바일 - 지도 네비게이션 (Mobile Map Navigation)

**경로:** `/mobile/map?session={sessionId}`

**레이아웃 구성:**
```
┌────────────────────────────┐
│ Header (56px)              │
│ ←     어디다이소            │
├────────────────────────────┤
│                            │
│                            │
│       Map Area             │
│       (fill)               │
│                            │
│     🔵 현재 위치            │
│                            │
│     📍 상품 위치            │
│                            │
│                            │
├────────────────────────────┤
│ Bottom Sheet (200px)       │
│ ┌────────────────────────┐ │
│ │ ┌────┐                 │ │
│ │ │ 📦 │ 규조토 욕실 매트│ │
│ │ └────┘ B1-B01 약30초소요│ │
│ │                        │ │
│ │ ┌──────────┐ ┌────────┐│ │
│ │ │🗺️지도보기│ │📷AR네비││ │
│ │ └──────────┘ └────────┘│ │
│ └────────────────────────┘ │
└────────────────────────────┘
```

**핵심 컴포넌트:**
- `MobileHeader`: 뒤로가기 + 브랜드 로고
- `MapView`: 실내 지도 + 경로 표시
- `BottomSheet`: 상품 정보 + 액션 버튼
- `ActionButton`: 지도 보기 / AR 네비 버튼

---

## 5. API 연동 설계

### 5.1 API 엔드포인트

| Method | Endpoint | 설명 | Request | Response |
|--------|----------|------|---------|----------|
| POST | `/api/v1/search` | 상품 검색 | `{ query, top_k }` | 검색 결과 |
| GET | `/api/v1/location/{product_id}` | 상품 위치 조회 | - | 위치 정보 |
| POST | `/api/v1/session` | 세션 생성 | `{ product_id }` | QR 페이로드 |
| GET | `/api/v1/navigate/{session_id}` | 네비게이션 데이터 | - | 경로 정보 |

### 5.2 TypeScript 타입 정의

```typescript
// types/search.ts
export interface SearchRequest {
  query: string;
  top_k?: number;
}

export interface SearchResponse {
  status: 'success' | 'not_found';
  intent: {
    type: 'product_location' | 'category_browse' | 'general';
    keywords: string[];
  };
  results: {
    top_1: Product | null;
    side_products: Product[];
  };
}

// types/product.ts
export interface Product {
  product_id: string;
  name: string;
  brand?: string;
  price: number;
  image_url?: string;
  confidence: number;
  location: ProductLocation;
  location_guide: string;
}

export interface ProductLocation {
  floor: 'B1' | 'B2' | '1F' | '2F';
  zone: string;
  shelf_id: string;
  coordinates: { x: number; y: number };
}

// types/location.ts
export interface NavigationData {
  session_id: string;
  product: Product;
  path: PathPoint[];
  estimated_time: number; // seconds
  qr_payload: QRPayload;
}

export interface QRPayload {
  type: 'product_location';
  product_id: string;
  session_id: string;
  deep_link: string;
}
```

### 5.3 API 서비스 구현

```typescript
// services/searchService.ts
import { apiClient } from './api';
import type { SearchRequest, SearchResponse } from '@/types/search';

export const searchService = {
  async search(request: SearchRequest): Promise<SearchResponse> {
    const response = await apiClient.post<SearchResponse>('/api/v1/search', {
      query: request.query,
      top_k: request.top_k ?? 5,
    });
    return response.data;
  },
};
```

---

## 6. 주요 기능 구현 가이드

### 6.1 음성 인식 (Web Speech API)

```typescript
// hooks/useVoiceInput.ts
import { useState, useCallback, useRef } from 'react';

export function useVoiceInput() {
  const [transcript, setTranscript] = useState('');
  const [isListening, setIsListening] = useState(false);
  const recognitionRef = useRef<SpeechRecognition | null>(null);

  const startListening = useCallback(() => {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    
    if (!SpeechRecognition) {
      throw new Error('Web Speech API not supported');
    }

    const recognition = new SpeechRecognition();
    recognition.lang = 'ko-KR';
    recognition.continuous = false;
    recognition.interimResults = true;

    recognition.onresult = (event) => {
      const current = event.resultIndex;
      const result = event.results[current][0].transcript;
      setTranscript(result);
    };

    recognition.onerror = (event) => {
      console.error('Speech recognition error:', event.error);
      setIsListening(false);
    };

    recognition.onend = () => {
      setIsListening(false);
    };

    recognitionRef.current = recognition;
    recognition.start();
    setIsListening(true);
  }, []);

  const stopListening = useCallback(() => {
    recognitionRef.current?.stop();
    setIsListening(false);
  }, []);

  return { transcript, isListening, startListening, stopListening };
}
```

### 6.2 QR 코드 생성

```typescript
// hooks/useQRCode.ts
import QRCode from 'qrcode';
import { useState, useCallback } from 'react';

export function useQRCode() {
  const [qrDataUrl, setQrDataUrl] = useState<string>('');

  const generateQR = useCallback(async (payload: QRPayload) => {
    const deepLink = `daiso://navigate?pid=${payload.product_id}&sid=${payload.session_id}`;
    const dataUrl = await QRCode.toDataURL(deepLink, {
      width: 200,
      margin: 2,
      color: {
        dark: '#E60012',
        light: '#FFFFFF',
      },
    });
    setQrDataUrl(dataUrl);
    return dataUrl;
  }, []);

  return { qrDataUrl, generateQR };
}
```

### 6.3 상태 관리 (Zustand)

```typescript
// store/useSearchStore.ts
import { create } from 'zustand';
import type { SearchResponse, Product } from '@/types';

interface SearchState {
  query: string;
  results: SearchResponse | null;
  selectedProduct: Product | null;
  isLoading: boolean;
  
  setQuery: (query: string) => void;
  setResults: (results: SearchResponse) => void;
  selectProduct: (product: Product) => void;
  clearResults: () => void;
}

export const useSearchStore = create<SearchState>((set) => ({
  query: '',
  results: null,
  selectedProduct: null,
  isLoading: false,

  setQuery: (query) => set({ query }),
  setResults: (results) => set({ results, selectedProduct: results.results.top_1 }),
  selectProduct: (selectedProduct) => set({ selectedProduct }),
  clearResults: () => set({ query: '', results: null, selectedProduct: null }),
}));
```

---

## 7. 검증 계획 (Verification Plan)

### 7.1 개발 환경 테스트

```bash
# 개발 서버 실행
npm run dev

# 접속 URL
# Kiosk: http://localhost:3000
# Mobile: http://localhost:3000/mobile
```

### 7.2 화면별 검증 체크리스트

| 화면 | 검증 항목 | 수동 테스트 방법 |
|------|----------|------------------|
| Voice Home | 레이아웃 1280x800 | 브라우저 개발자도구에서 해상도 설정 |
| Voice Home | 마이크 버튼 동작 | 버튼 클릭 → voice-input 페이지 이동 확인 |
| Voice Input | 음성 인식 | 마이크 버튼 클릭 → 한국어 발화 → 텍스트 표시 확인 |
| Search Results | API 연동 | 검색어 입력 → 결과 카드 렌더링 확인 |
| Search Results | 지도 표시 | 상품 선택 → B1/B2 지도에 위치 마커 표시 |
| Category Map | 카테고리 필터 | 사이드바 버튼 클릭 → 해당 영역 강조 |
| Mobile Map | QR 스캔 후 진입 | QR 코드 생성 → 모바일 브라우저에서 스캔 → 지도 표시 |

### 7.3 자동화 테스트 (예정)

```bash
# Jest + React Testing Library
npm run test

# Playwright E2E 테스트
npm run test:e2e
```

---

## 8. 의존성 패키지

```json
{
  "dependencies": {
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.3.0",
    "tailwindcss": "^3.4.0",
    "zustand": "^4.5.0",
    "axios": "^1.6.0",
    "qrcode": "^1.5.0",
    "lucide-react": "^0.320.0",
    "framer-motion": "^11.0.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/qrcode": "^1.5.0",
    "eslint": "^8",
    "eslint-config-next": "14.1.0",
    "postcss": "^8",
    "autoprefixer": "^10"
  }
}
```

---

## 9. 구현 우선순위 및 일정

### Phase 1: 기본 구조 및 홈 화면 (Day 1-2)
- [ ] Next.js 프로젝트 초기화
- [ ] 디자인 시스템 (CSS 변수, Tailwind 설정)
- [ ] 공통 컴포넌트 (Header, Button, Card)
- [ ] 홈 화면 구현

### Phase 2: 검색 기능 (Day 3-4)
- [ ] 음성 입력 화면
- [ ] 검색 API 연동
- [ ] 검색 결과 화면
- [ ] 상품 카드 및 지도 표시

### Phase 3: 위치 안내 (Day 5-6)
- [ ] 카테고리 지도 화면
- [ ] QR 코드 생성 및 표시
- [ ] 결과 없음 화면

### Phase 4: 모바일 연동 (Day 7-8)
- [ ] 모바일 레이아웃
- [ ] 지도 뷰
- [ ] AR 네비게이션 (기본)

### Phase 5: 테스트 및 최적화 (Day 9-10)
- [ ] 반응형 테스트
- [ ] 성능 최적화
- [ ] 접근성 검토

---

## 참고 문서

- [RAG 시스템 아키텍처](./ux-ui/rag_system_architecture.md)
- [다이소 UI v4 디자인 가이드](./ux-ui/02-05/daiso-ui-v4.md)
- [Pencil UI 디자인 파일](../code_tracker/active/no_repo/29f3af7de1eb9b0256a3588a12f20a21_daiso-kiosk_v3.pen)
